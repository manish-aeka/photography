CLOUDINARY UPLOAD FLOW
═════════════════════════════════════════════════════════════

BEFORE (Base64)                    AFTER (Cloudinary)
═══════════════                    ══════════════════

┌─────────────┐                   ┌─────────────┐
│ User clicks │                   │ User clicks │
│   Upload    │                   │   Upload    │
└──────┬──────┘                   └──────┬──────┘
       │                                 │
       ▼                                 ▼
┌─────────────┐                   ┌─────────────┐
│ Select file │                   │ Select file │
└──────┬──────┘                   └──────┬──────┘
       │                                 │
       ▼                                 ▼
┌─────────────┐                   ┌─────────────────────┐
│ FileReader  │                   │ Validate file       │
│             │                   │ - Size (10MB max)   │
│ Read file   │                   │ - Format (jpg/png)  │
└──────┬──────┘                   └──────┬──────────────┘
       │                                 │
       ▼                                 ▼
┌─────────────┐                   ┌─────────────────────┐
│Convert to   │                   │ Show progress:      │
│  base64     │                   │ "Uploading... 0%"   │
└──────┬──────┘                   └──────┬──────────────┘
       │                                 │
       │                                 ▼
       │                          ┌─────────────────────┐
       │                          │ Upload to Cloudinary│
       │                          │ with XMLHttpRequest │
       │                          └──────┬──────────────┘
       │                                 │
       │                                 ▼
       │                          ┌─────────────────────┐
       │                          │ Track progress:     │
       │                          │ 0% → 25% → 50%     │
       │                          │ → 75% → 100%       │
       │                          └──────┬──────────────┘
       │                                 │
       ▼                                 ▼
┌─────────────┐                   ┌─────────────────────┐
│ Save base64 │                   │ Receive URL from    │
│ in JSON     │                   │ Cloudinary          │
│             │                   │ (100 bytes)         │
│ (300KB+)    │                   └──────┬──────────────┘
└──────┬──────┘                          │
       │                                 ▼
       │                          ┌─────────────────────┐
       │                          │ Show success:       │
       │                          │ "Upload complete!"  │
       │                          │ (green checkmark)   │
       │                          └──────┬──────────────┘
       │                                 │
       ▼                                 ▼
┌─────────────┐                   ┌─────────────────────┐
│   Done!     │                   │ Save URL in JSON    │
│             │                   │ (100 bytes)         │
└─────────────┘                   └──────┬──────────────┘
                                         │
                                         ▼
                                  ┌─────────────────────┐
                                  │   Done!             │
                                  │                     │
                                  │ Image on Cloudinary │
                                  │ CDN (fast delivery) │
                                  └─────────────────────┘


FILE SIZE COMPARISON
════════════════════

Base64 in JSON:
┌──────────────────────────────────────┐
│ data:image/jpeg;base64,/9j/4AAQSk... │  ← 300KB per image
│ ...thousands of characters...        │
└──────────────────────────────────────┘

Cloudinary URL in JSON:
┌──────────────────────────────────────┐
│ https://res.cloudinary.com/my-cloud/ │  ← 100 bytes
│ image/upload/v1234/photo.jpg         │
└──────────────────────────────────────┘

10 IMAGES:
  Base64:     3 MB in JSON file ❌
  Cloudinary: 1 KB in JSON file ✅


PROGRESS TRACKING
═════════════════

User Experience:

1. Click Upload
   ▼
2. Select image (photo.jpg, 2.5 MB)
   ▼
3. Progress bar appears:
   
   ┌────────────────────────────────────┐
   │ Uploading slider image 1...        │
   │                                    │
   │ ████████░░░░░░░░░░░░░░░░░░░░  35% │
   │                                    │
   └────────────────────────────────────┘
   
   Updates in real-time: 0% → 35% → 67% → 100%
   
4. Success notification:
   
   ┌────────────────────────────────────┐
   │ Upload complete!                   │
   │                                    │
   │ ████████████████████████████  100% │
   │                                    │
   └────────────────────────────────────┘
   
   (Green background, auto-hides after 2s)

5. URL saved in JSON automatically


EDITOR INTEGRATION
══════════════════

All 7 upload functions updated:

editor.html
├── pickImageForAboutCard()     ✅ Cloudinary + Progress
├── pickImageForLogo()          ✅ Cloudinary + Progress
├── pickImageForFallback()      ✅ Cloudinary + Progress
├── pickImageForSlider(i)       ✅ Cloudinary + Progress
├── pickImageForCategory(i)     ✅ Cloudinary + Progress
├── pickCategoryImage(ci, ii)   ✅ Cloudinary + Progress
└── pickImageForGallery(i)      ✅ Cloudinary + Progress

Helper functions added:
├── showUploadProgress(msg, %)  ✅ Show progress bar
└── hideUploadProgress()        ✅ Hide progress bar


CLOUDINARY API CALL
═══════════════════

What happens when you upload:

Browser                          Cloudinary
┌──────┐                        ┌──────────┐
│      │  POST with FormData    │          │
│      │  ────────────────────> │          │
│      │                        │          │
│      │  File: photo.jpg       │  Stores  │
│      │  Preset: my_preset     │  image   │
│      │  Folder: portfolio     │          │
│      │                        │          │
│      │  Progress events       │          │
│      │  <────────────────────┤          │
│      │  0% ... 50% ... 100%   │          │
│      │                        │          │
│      │  Response JSON         │          │
│      │  <────────────────────┤          │
│      │  {                     │          │
│      │    url: "https://...", │          │
│      │    publicId: "...",    │          │
│      │    width: 1920         │          │
│      │  }                     │          │
└──────┘                        └──────────┘


CONFIGURATION CHECKLIST
═══════════════════════

□ 1. Create Cloudinary account (cloudinary.com)
□ 2. Get Cloud Name from dashboard
□ 3. Create Upload Preset:
     □ Go to Settings → Upload
     □ Click "Add upload preset"
     □ Set Signing Mode: "Unsigned"
     □ Set Folder: "photography-portfolio"
     □ Save and note preset name
□ 4. Update cloudinary-uploader.js (line 196-200):
     □ Replace cloudName
     □ Replace uploadPreset
□ 5. Test with test-cloudinary.html
□ 6. Start using editor!


ERROR HANDLING
══════════════

✓ File too large (>10MB)
  → Alert: "File size exceeds 10MB limit"
  
✓ Wrong format (.pdf, .txt)
  → Alert: "File format not supported. Use JPEG, PNG, WebP or GIF"
  
✓ Network error
  → Alert: "Upload error: Network error during upload"
  → Progress bar shows error (red)
  
✓ Invalid credentials
  → Alert: "Upload failed: Invalid preset"
  → Check cloudName and uploadPreset


MIGRATION STRATEGY
══════════════════

Option 1: Keep base64 images
  → Old images continue to work
  → New uploads go to Cloudinary
  → Gradual migration over time

Option 2: Migrate all at once
  → Open editor
  → Re-upload each image
  → Save JSON
  → Much smaller JSON file!

Both work! Your choice based on needs.

═══════════════════════════════════════════════════════════
Ready to go! Configure and start uploading to Cloudinary! 🚀
═══════════════════════════════════════════════════════════
